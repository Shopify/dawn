<style>
  * {
    box-sizing: border-box;
    margin: 0;
    padding: 0;
  }

  body {
    font-family: var(--font-ltcushion), sans-serif;
    background-color: var(--old-lace);
  }

  strong {
    font-weight: 700;
  }

  h1,
  h2,
  h3,
  h4,
  a,
  button {
    font-family: var(--font-unbounded), sans-serif;
    font-weight: normal;
  }

  p {
    font-family: var(--font-ltcushion), sans-serif;
    font-size: clamp(12px, 16px, 20px);
    color: var(--onyx);
    line-height: 1.2;
    letter-spacing: 0px;
  }

  .hero {
    display: flex;
    flex-direction: row;
    justify-content: space-between;
    align-items: center;
    padding: 100px 10dvw;
    background-color: var(--old-lace);
    flex-wrap: wrap;
    height: 90dvh;
    width: 100%;
  }

  .hero-content {
    display: flex;
    flex-direction: column;
    justify-content: center;
    gap: 30px;
    width: 100%;
    max-width: 1130px;
    flex: 1;
  }

  .hero-title {
    display: inline-block;
    position: relative;
    font-size: clamp(2.5rem, 3vw, 48px);
    color: var(--onyx);
    font-weight: 300;
  }

  .hero-description {
    font-size: clamp(4rem, 6vw, 90px);
    font-weight: 300;
    color: var(--onyx);
    line-height: 1.1;
  }

  .hero-text {
    font-size: clamp(14px, 20px, 20px);
    color: var(--onyx);
  }

  .bakedream-btn {
    display: flex;
    justify-content: center;
    align-items: center;
    padding: clamp(5px, 10px, 15px) clamp(10px, 20px, 30px);
    font-weight: 500;
    text-decoration: none;
    border-radius: 8px;
    gap: 10px;
    border: 2px solid transparent;
    width: fit-content;
    font-size: clamp(12px, 14px, 16px);
  }

  .btn-primary {
    background-color: var(--chocolate-cosmos);
    color: var(--old-lace);
    transition: transform 0.3s ease;
    border: 3px solid var(--chocolate-cosmos);
    transition: background-color 0.3s ease, color 0.3s ease;
  }

  .btn-primary:hover {
    color: var(--chocolate-cosmos);
    background-color: var(--old-lace);
  }

  .hero-image {
    max-width: 600px;
    width: 100%;
    height: auto;
    flex: 1;
    margin-top: clamp(0px, 5vw, 20px);
    transform: scaleX(-1);
  }

  @media (max-width: 1024px) {
    .hero {
      padding: 100px 40px;
      & .hero-content {
        gap: 40px;
      }
    }
    .hero img {
      display: none;
    }

    .hero-content {
      justify-content: center;
      align-items: center;
      text-align: center;
    }
  }
</style>

<div class="hero">
  <div class="hero-content">
    <h3 class="hero-title">we deliver</h3>
    <h1 class="hero-description">
      your <strong>cookie</strong><br>
      to your <strong>door</strong>.
    </h1>
    <p class="hero-text">
      Discover our handcrafted cookies—crispy on the outside, soft on the <br>
      inside—made with love in New York.
    </p>
    <a href="/collections/all" class="bakedream-btn btn-primary"> order now <i data-lucide="cookie"></i></a>
  </div>
  <img
    src="{{ 'hero-image.png' | asset_url }}"
    alt="hero image"
    class="hero-image"
    loading="lazy"
  >
</div>
<script>
  /* GSAP Animations JS */
  document.addEventListener('DOMContentLoaded', () => {
    gsap.registerPlugin(ScrollTrigger);

    const tl = gsap.timeline({
      scrollTrigger: {
        trigger: '.hero',
        start: 'top 40%',
        toggleActions: 'play reset play reverse',
      },
      defaults: { ease: 'power1.out', duration: 0.7 },
    });

    tl.fromTo('.hero-title', { opacity: 0 }, { opacity: 1 })
      .fromTo('.hero-description', { y: 100, opacity: 0 }, { y: 0, opacity: 1 }, '-=0.2')
      .fromTo('.hero-text', { opacity: 0 }, { opacity: 1 }, '-=0.2')
      .fromTo('.hero-image', { y: 100, opacity: 0 }, { y: 0, opacity: 1 }, '-=0.5s')
      .fromTo('.bakedream-btn', { opacity: 0 }, { opacity: 1 }, '-=0.5');
  });
</script>
