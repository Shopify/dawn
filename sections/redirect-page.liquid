 {%- if customer == null -%}
    <script>
    const currentUrl = window.location.href;
    const loginUrl = "https://online.sukiya.biz/account/login?return_url=" + encodeURIComponent(currentUrl);
    if (!(window.frameElement && window.frameElement.id && window.frameElement.id === 'theme-editor-iframe')) {
      window.location.href = loginUrl;
    }
  </script>
  {%- endif -%}