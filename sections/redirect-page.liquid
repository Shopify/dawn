{%- if customer == null -%}
  <script>
    const currentUrl = window.location.href;
    const loginUrl = window.location.origin + "/account/login?return_url=" + encodeURIComponent(currentUrl);
    if (!(window.frameElement && window.frameElement.id && window.frameElement.id === 'theme-editor-iframe')) {
      window.location.href = loginUrl;
    }
  </script>
{%- endif -%}

{% schema %}
  {
    "name": "Redirect Page",
    "settings": [],
    "presets": [
      {
        "name": "Redirect Page"
      }
    ]
  }
{% endschema %}
