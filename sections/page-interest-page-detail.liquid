{{ 'interest-page.css' | asset_url | stylesheet_tag }}
{%- assign metaValue = page.metafields.custom.interest_page.value -%}

<div
  {% if section.settings.id_name != blank %}id="{{ section.settings.id_name }}"{% endif %}
  class="interest-page"
>
  <div class="interest-page-key-visual">
    <div class="interest-page-key-visual-body">
      <h1 class="interest-page-key-visual-title">{{ metaValue.title.value }}</h1>
      <p class="interest-page-key-visual-intro">{{ metaValue.intro.value }}</p>
      <div class="interest-page-key-visual-image">
        <img src="{{ metaValue.key_visual.value | img_url: '1000x500' }}" alt="" />
      </div>
    </div>
  </div>
  <div class="interest-page-content">
    {{ page.content }}
  </div>
</div>

{% schema %}
  {
    "name": "Interest Page Detail",
    "settings": []
  }
{% endschema %}