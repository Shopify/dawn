{% comment %}
  Tiktok reel

  Branded content
  Renders influencer reel for PDP.
  Accepts:
  - product: {Object} product object.

  Usage:
  {% render 'product-reel', product:product  %}
{% endcomment %}

<div class="flex flex-col gap-normal desktopUp:flex-row desktopUp:gap-huge items-center">
  {% assign product_reel = product.metafields.custom.product_reel.value %}
  <div class="w-full rounded-[54px] h-auto">
    {{
      product_reel.media.value
      | video_tag:
        image_size: '640x',
        autoplay: true,
        loop: true,
        controls: true,
        muted: true,
        class: 'w-full rounded-[54px] h-auto'
    }}
  </div>

  <div class="w-full flex flex-col gap-base overflow-hidden">
    <h2 class="text-h-md text-sky italic uppercase text-center">
      {{ product_reel.headline }}
    </h2>

    <p class="text-body-xs text-grey500">
      {{ product_reel.author_quote }}
    </p>

    <a href="{{ product_reel.author.url  }}"> {{ product_reel.author.title }} </a>
  </div>
</div>
