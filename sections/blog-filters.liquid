{% schema %}
{
  "name": "Blog Filters",
  "settings": [],
  "blocks": [
    {
      "type": "filter_tag",
      "name": "Filter Tag",
      "settings": [
        { "type": "text", "id": "tag", "label": "Tag (used for filtering posts)" },
        { "type": "image_picker", "id": "banner_image", "label": "Banner Image" },
        { "type": "color", "id": "banner_color", "label": "Banner Color", "default": "#e5e0f7" },
        { "type": "text", "id": "headline", "label": "Headline Text" }
      ]
    }
  ],
  "max_blocks": 20,
  "limit": 1,
  "enabled_on": {
    "templates": ["blog"]
  }
}
{% endschema %}
