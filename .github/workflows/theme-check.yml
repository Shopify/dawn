name: Theme Check

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  theme-check:
    name: Theme Check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Theme Check
        uses: andershagbard/theme-check-action@package-manager-support
        with:
          token: ${{ github.token }}
          base: main