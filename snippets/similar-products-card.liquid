<div class="product-card">
  <div class="product-card__image">
    <img
      height="100"
      width="100"
      src="https://via.placeholder.com/300x200"
      alt="{{ product_name | default: 'Product' }}"
    >
  </div>
  <div class="product-card__info">
    <h3 class="product-card__title">{{ product_name | default: 'PRODUCT NAME' }}</h3>
    <p class="product-card__price">{{ product_price | default: '£239' }}</p>
  </div>
</div>
