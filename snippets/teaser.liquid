{% comment %}
  Renders a large teaser item.
  Accepts:
  - image: {Object} teaser image url
  - headline: {String} Teaser headline
  - copy : {Richtext} Teaser copy
  - price: {Number} Teaser price

  Usage:
  {% render 'teaser',
    image: image,
    headline: headline,
    copy: copy,
    price: price
  %}
{% endcomment %}

{% liquid
  assign money = price | money_without_currency
%}

<div>
  <div class="flex flex-col md:flex-row w-full">
    <div class="w-full aspect-[131_/_100] md:aspect-[32_/_45] xl:aspect-square 5kUp:aspect-[35_/_27]">
      {%- if image -%}
        {%- liquid
          assign sizes = '100vw'
          assign widths = '375, 550, 750, 1100, 1500, 1780, 2000, 3000, 3840'
          assign fetch_priority = 'auto'
        -%}
        {{
          image
          | image_url: width: 3840
          | image_tag:
            loading: 'lazy',
            height: height,
            sizes: sizes,
            widths: widths,
            fetchpriority: fetch_priority,
            class: 'w-full h-full object-fill'
        }}
      {%- else -%}
        {{ 'hero-apparel-1' | placeholder_svg_tag: 'placeholder-svg' }}
      {%- endif -%}
    </div>

    <div class="text-teal p-large bg-sky flex flex-col justify-between aspect-[131_/_100] md:aspect-[32_/_45] xl:aspect-square 5kUp:aspect-[35_/_27]">
      <h1 class="text-mb-lg md:text-h-lg">
        {{ headline }}
      </h1>

      <div class="w-full flex justify-between items-center">
        <div class="text-t-sm">
          {{ copy }}
        </div>

        <div class="w-[54px] h-[54px] md:w-mega md:h-mega rounded-full bg-teal text-sky flex items-center justify-center text-h-sm md:text-h-md truncate">
          <span>
            {{ money }}
          </span>
        </div>
      </div>
    </div>
  </div>
</div>
