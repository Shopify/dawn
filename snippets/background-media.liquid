{% liquid
  if background_media == 'video'
    render 'background-video', background_video: background_video, background_video_position: background_video_position, section_id: section_id, block_id: block_id
  elsif background_media == 'image'
    render 'background-image', background_image: background_image, background_image_position: background_image_position, section_id: section_id, block_id: block_id
  endif
%}
