{% comment %} Минимальная сумма для бесплатной доставки (в центах) {% endcomment %}

{{ 'shipping-bar.css' | asset_url | stylesheet_tag }}
{% assign free_shipping_minimum = 100000 %}

<div
  class="free-shipping-bar"
  data-shipping-minimum="{{ free_shipping_minimum }}"
  data-cart-total="{{ cart.total_price }}"
>
  <div class="progress-container">
    <div
      class="progress-fill"
      style="width: {{ cart.total_price | times: 100 | divided_by: free_shipping_minimum | at_most: 100 }}%;"
    ></div>
  </div>
  <p class="progress-text">
    {% if cart.total_price >= free_shipping_minimum %}
      🎉 Бесплатная доставка активирована!
    {% else %}
      Добавьте еще <strong>{{ free_shipping_minimum | minus: cart.total_price | money }}</strong> для бесплатной
      доставки.
    {% endif %}
  </p>
</div>
