
        {% layout 'theme.zeno.fullscreen' %}

        <div class="zn-content">
          {% assign articleKey = 'zeno-article-' | append: article.id %}
          <link rel="stylesheet" href="{{ articleKey | append: '.css' | asset_url }}">
          {% include articleKey %}
          <script src="{{ articleKey | append: '.js' | asset_url }}"></script>
        </div>
      